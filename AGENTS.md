# AGENTS.md

**Read [`docs/AGENTS.md`](docs/AGENTS.md) before doing any work in this repo — it is
the canonical guide (project rules, work-intake funnel, verification, house style).**
Then read the house rules it points to in `~/code/jon-platform` (start with its
`AGENTS.md`). If you cannot access `~/code/jon-platform`, stop and tell Jon.

This root file exists only so agents that auto-load `AGENTS.md` from the repository
root land on the canonical guide. Do not duplicate rules here — add them to
`docs/AGENTS.md` so there is one source of truth.
